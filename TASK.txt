Current page: Router

Brief:

    Currently making watch_telegram solely just watch and filter.

    It will then send to SRouter, which will determine whether it's 
    a hardcoded command, or for LLM.

    Hardcoded commands are finished, haven't ran it yet, everything isn't linked up yet.

    Need to link LLM to router somehow and let LLM be able to call actions through it. 
    Right now there will be simple gateways, just whitelist, admin, unknown and blacklist.
    This will be run in private for now. so all actions are accessible.

    TO link up the LLM, i need to take ideas from MarkX, with intents and function callings.


Current working flowchart:

w_telegram --> SRouter --> fastCMD --> actions(e.g.s_telegram)
                |                             ^
                -> available actions --> LLM -|


LONG TERM TASK:
    - Finish level 1 of perlica


Notes:
    - In send telegram, there will be 2 bots. console and main.


Prioirity task:
    [-] add print() on every step to understand what's going on!!!! :))))))) fucking helll
    [-] try out the bot. It should output the json file it outputs


    [-] actually run mentimeter spam bot
    [-] add one more intent, "CheckList" for daily checklist for emails, etc.
        so it won't send texts or sum idk. it's special.

Small tasks:
    

Finished Tasks:
    [v] move out health
    [v] finish redesigning w_telegram
    
    [v] connect id to actions etc. basically an object carrying id around so it knows what it's doing 

    [v] create send telegram action
    [v] finish router [missing LLM]
    [v] finish fastCMD
    [v] write all txt file in core

    [v] LLM.py still need to hook up everything.
    
    [v] explore queuing

Upcoming tasks:
    - make watch_telegram asyncio or sum idk. figure it out. it's stuck in loop